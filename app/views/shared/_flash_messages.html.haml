- flash.each do |type, message|
  %div{:class => "alert #{alert_class_for(type)} alert-dismissible fade in flash-width"}
    %button.close{"data-dismiss" => "alert", :type => "button"}
      %span{"aria-hidden" => "true"} ×
      %span.sr-only Close
    = message

